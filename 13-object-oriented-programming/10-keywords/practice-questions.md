1. What is interface in Java?
2. Difference between abstract class and interface?
3. Can interface have methods with body?
4. What is static keyword?
5. What is super keyword?
6. When do we use super()?