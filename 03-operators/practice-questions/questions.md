## Practice Questions — Operators

1️⃣ Input 2 numbers and print:
   - sum
   - difference
   - product
   - quotient

2️⃣ Input a number and print:
   - "Positive" if > 0
   - "Negative" if < 0
   - "Zero" if == 0
   (Use ternary operator)

3️⃣ What will be the output?
   int a = 10;
   System.out.println(a++ + ++a);

4️⃣ Check if a person can vote:
   age >= 18 AND has ID card

5️⃣ Predict the type & output:
   int a = 5;
   double b = 2;
   System.out.println(a / b);
