public class ArrayDefinition {
    public static void main(String[] args) {
        // An array is a container for multiple values of the same type
        int[] numbers = new int[5]; // fixed size
        System.out.println("Length: " + numbers.length);
    }
}
